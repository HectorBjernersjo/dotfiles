(defcfg
    process-unmapped-keys yes
)

(defsrc)

(deflayer base)

(defoverrides
  (lalt h) (left)
  (lalt j) (down)
  (lalt k) (up)
  (lalt l) (rght)
  (lalt a) (end)
  (lalt i) (home)
  (lalt d) (del)
  (lalt p) (bspc)
  (lalt scln) (ret)
  (caps) (esc)
)
